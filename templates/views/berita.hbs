<!DOCTYPE html>

<html>
    <head>
        <title>Berita</title>
        <link rel="icon" href="/img/awan.png">
        <link rel="stylesheet" href="/css/style.css">
  </head>
  <body>
    {{>header}}

    <div class="main-content-berita">
        <div class="berita-list">
            {{#each berita}}
            <div class="berita-item">
                <h3>{{title}}</h3>
                <img class = "image-berita" src="{{image}}"/>
                <p >{{description}}</p>
                <a href="{{url}}" target="_blank">Baca selengkapnya</a>
               
            </div>
            {{/each}}
        </div>
    </div>
    {{>footer}}
</body>
</html>